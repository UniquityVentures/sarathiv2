<odoo>
  <data>
    <!-- explicit list view definition -->
    <record model="ir.ui.view" id="section.list">
      <field name="name">section list</field>
      <field name="model">section.section</field>
      <field name="arch" type="xml">
        <list>
          <field name="name"/>
        </list>
      </field>
    </record>

    <!-- actions opening views on models -->
    <record model="ir.actions.act_window" id="section.action_window">
      <field name="name">section window</field>
      <field name="res_model">section.section</field>
      <field name="view_mode">list,form</field>
    </record>

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="section.action_server">
      <field name="name">section server</field>
      <field name="model_id" ref="model_section_section"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "list,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
    <menuitem name="Section" id="section.menu_root"/>
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="section.menu_1" parent="section.menu_root"/>
    <menuitem name="Menu 2" id="section.menu_2" parent="section.menu_root"/>
-->
    <!-- actions -->
    <menuitem name="List" id="section.menu_1_list" parent="section.menu_root"
              action="section.action_window"/>
<!--
    <menuitem name="Server to list" id="section" parent="section.menu_2"
              action="section.action_server"/>
-->
  </data>
</odoo>
